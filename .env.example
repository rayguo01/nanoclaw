# NanoClaw Configuration

# Claude API (required - pick one)
# Option 1: Use OAuth (for Claude Code users)
CLAUDE_CODE_OAUTH_TOKEN=your-oauth-token
# Option 2: Use API key directly
ANTHROPIC_API_KEY=your-api-key

# Assistant name (trigger word for non-main groups)
ASSISTANT_NAME=Andy

# Container settings
CONTAINER_IMAGE=nanoclaw-agent:latest
CONTAINER_TIMEOUT=300000

# Web UI (optional - set both to enable)
WEB_PASSWORD=your-secure-password
WEB_PORT=3000
WEB_JWT_SECRET=random-32-character-string-here

# Nango OAuth (for Google Calendar and other integrations)
NANGO_SECRET_KEY=from-nango-dashboard
NANGO_PUBLIC_KEY=from-nango-dashboard
NANGO_SERVER_URL=https://your-domain.com/nango
NANGO_CALLBACK_URL=https://your-domain.com/nango/oauth/callback

# Logging
LOG_LEVEL=info
